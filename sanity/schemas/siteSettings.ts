import { defineField, defineType } from 'sanity';
const image = (title: string) => defineField({ name: 'logo', title, type: 'image', options: { hotspot: true }, fields: [defineField({ name: 'alt', title: 'Alternativtext', type: 'string' })] });
export const siteSettings = defineType({ name: 'siteSettings', title: 'Website-Einstellungen', type: 'document', fields: [defineField({ name: 'siteName', title: 'Website-Name', type: 'string', validation: (Rule) => Rule.required() }), image('Logo'), defineField({ name: 'email', title: 'E-Mail', type: 'string', validation: (Rule) => Rule.email() }), defineField({ name: 'phone', title: 'Telefon', type: 'string' }), defineField({ name: 'instagram', title: 'Instagram URL', type: 'url' }), defineField({ name: 'linkedin', title: 'LinkedIn URL', type: 'url' }), defineField({ name: 'donationUrl', title: 'Spenden-URL', type: 'url' }), defineField({ name: 'footerText', title: 'Footer-Text', type: 'string' })], preview: { prepare: () => ({ title: 'Website-Einstellungen' }) } });
