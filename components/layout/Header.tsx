'use client';
import Link from 'next/link'; import {Menu, X} from 'lucide-react'; import {useState} from 'react';
const links=[['Über uns','/ueber-uns'],['Netzwerk','/netzwerk'],['Veranstaltungen','/veranstaltungen'],['Nachwuchs','/nachwuchs'],['Presse','/presse']];
export function Header(){const [open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-[var(--line)] bg-[var(--paper)]"><div className="container flex h-[76px] items-center justify-between"><Link href="/" className="serif text-xl font-bold tracking-tight">Führungsfrauen<span className="text-[var(--rose)]">.</span></Link><nav className="desktop-nav flex items-center gap-7">{links.map(([n,h])=><Link className="navlink" href={h} key={h}>{n}</Link>)}<Link className="navlink text-[var(--rose)]" href="/spenden">Spenden</Link><Link className="btn" href="/kontakt">Mitmachen</Link></nav><button aria-label="Menü öffnen" className="menu" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<nav className="mobile-menu hidden absolute top-[76px] w-full border-b border-[var(--line)] bg-[var(--paper)] p-6 gap-5">{links.map(([n,h])=><Link onClick={()=>setOpen(false)} className="text-2xl serif" href={h} key={h}>{n}</Link>)}<Link href="/spenden">Spenden</Link><Link href="/kontakt">Mitmachen</Link></nav>}</header>}
