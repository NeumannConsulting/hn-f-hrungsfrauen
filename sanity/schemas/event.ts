import { defineField, defineType } from 'sanity';
const image = defineField({ name: 'image', title: 'Bild', type: 'image', options: { hotspot: true }, fields: [defineField({ name: 'alt', title: 'Alternativtext', type: 'string' })] });
export const event = defineType({ name: 'event', title: 'Veranstaltung', type: 'document', fields: [defineField({ name: 'title', title: 'Titel', type: 'string', validation: (Rule) => Rule.required() }), defineField({ name: 'slug', title: 'Slug', type: 'slug', options: { source: 'title', maxLength: 96 }, validation: (Rule) => Rule.required() }), defineField({ name: 'date', title: 'Datum und Uhrzeit', type: 'datetime', validation: (Rule) => Rule.required() }), defineField({ name: 'time', title: 'Zusätzliche Zeitanzeige', type: 'string' }), defineField({ name: 'location', title: 'Ort', type: 'string' }), defineField({ name: 'type', title: 'Kategorie', type: 'string', options: { list: ['Netzwerk', 'Nachwuchs', 'Austausch', 'Sonstiges'] }, validation: (Rule) => Rule.required() }), defineField({ name: 'excerpt', title: 'Kurztext', type: 'text', rows: 3 }), defineField({ name: 'description', title: 'Beschreibung', type: 'text', rows: 8 }), image, defineField({ name: 'registrationUrl', title: 'Anmelde-URL', type: 'url' }), defineField({ name: 'featured', title: 'Hervorgehoben', type: 'boolean', initialValue: false })] });
