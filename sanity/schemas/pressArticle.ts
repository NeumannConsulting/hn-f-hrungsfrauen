import { defineField, defineType } from 'sanity';
export const pressArticle = defineType({ name: 'pressArticle', title: 'Presseartikel', type: 'document', fields: [defineField({ name: 'title', title: 'Titel', type: 'string', validation: (Rule) => Rule.required() }), defineField({ name: 'slug', title: 'Slug', type: 'slug', options: { source: 'title', maxLength: 96 }, validation: (Rule) => Rule.required() }), defineField({ name: 'date', title: 'Datum', type: 'date', validation: (Rule) => Rule.required() }), defineField({ name: 'medium', title: 'Medium', type: 'string' }), defineField({ name: 'excerpt', title: 'Kurzbeschreibung', type: 'text', rows: 4 }), defineField({ name: 'url', title: 'Externer Link', type: 'url' }), defineField({ name: 'image', title: 'Bild', type: 'image', options: { hotspot: true }, fields: [defineField({ name: 'alt', title: 'Alternativtext', type: 'string' })] })] });
