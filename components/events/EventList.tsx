import Link from 'next/link'; import {ArrowUpRight} from 'lucide-react'; import type {Event} from '@/lib/types';
export function EventList({events}:{events:Event[]}){return <div>{events.map(e=>{const d=new Date(e.date);return <article className="event" key={e._id}><div><div className="eyebrow">{e.type}</div><div className="event-date">{Number.isNaN(d.valueOf())?'Termin folgt':d.toLocaleDateString('de-DE',{day:'2-digit',month:'short',year:'numeric'})}</div></div><div><h3 className="serif text-2xl">{e.title}</h3><p className="mt-2 text-[var(--muted)]">{e.location}{e.location&&e.excerpt?' · ':''}{e.excerpt}</p></div><Link aria-label={`${e.title} ansehen`} className="btn alt" href={`/veranstaltungen/${e.slug}`}><ArrowUpRight size={19}/></Link></article>})}</div>}
