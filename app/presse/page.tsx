import Link from 'next/link'; import { PageHero } from '@/components/ui'; import { getPressArticles } from '@/lib/sanity/data';
export const metadata = { title: 'Presse' };
export default async function PressPage() { const articles = await getPressArticles(); return <><PageHero eyebrow="Presse" title="Einblicke und Berichte." text="Presseanfragen richten Sie bitte an presse@fuehrungsfrauen-hn.de." /><section className="container"><div className="grid gap-6 md:grid-cols-3">{articles.map((article) => <Link className="card min-h-60 flex flex-col justify-between" href={`/presse/${article.slug}`} key={article._id}><div><p className="eyebrow">{new Date(article.date).getFullYear()} · {article.medium}</p><h2 className="serif mt-7 text-3xl">{article.title}</h2></div><p className="text-sm text-[var(--muted)]">{article.excerpt}</p></Link>)}</div></section></>; }
